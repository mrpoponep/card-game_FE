import React from 'react';
import ReactDOM from 'react-dom/client';
import MatchResultScreen from './MatchResultScreen.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <MatchResultScreen />
  </React.StrictMode>,
);

